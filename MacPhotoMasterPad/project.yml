name: MacPhotoMasterPad
options:
  bundleIdPrefix: photos.briansmith
  deploymentTarget:
    iOS: "17.0"

packages:
  MacPhotoMasterCore:
    path: ..

targets:
  MacPhotoMasterPad:
    type: application
    platform: iOS
    sources:
      - path: Sources
    dependencies:
      - package: MacPhotoMasterCore
        product: MacPhotoMasterCore
    # Local MLX (mlx:) inference peaks past the default ~5GB iOS per-process jetsam limit even for the
    # small FastVLM-0.5B model; this entitlement raises the ceiling so an M4 iPad (16GB) can use more
    # of its RAM. See docs/MLX_PROVIDER.md. Needs -allowProvisioningUpdates so automatic signing adds
    # the capability to the development profile.
    entitlements:
      path: MacPhotoMasterPad.entitlements
      properties:
        com.apple.developer.kernel.increased-memory-limit: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: photos.briansmith.macphotomasterpad
        PRODUCT_NAME: MacPhotoMasterPad
        DEVELOPMENT_TEAM: U4UCUZRYBD
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "2"
        SWIFT_VERSION: "5.0"
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        # `_iPad`, not the `~ipad` the plist key itself uses: this is a build setting, and Xcode's
        # allowlist (checked against CoreBuildSystem.xcspec) names only the underscore form. The
        # tilde form was accepted here in silence and dropped on the way out - the built Info.plist
        # carried no orientation key at all, which is what the all-orientations warning was about.
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown"
        # Process & Move writes into `Documents/ProcessedLibrary` (see PhotoBrowserViewModel
        # .libraryRootURL), and those files have to leave the device for the Mac app to finish them
        # off. Without both keys the folder is sealed inside the sandbox: file sharing exposes
        # `Documents` in Finder's Files tab over USB, and opening-in-place exposes the same folder to
        # the on-device Files app, from where they can be sent to a share or USB drive. The Files
        # app's "On My iPad" listing needs *both* — one alone shows nothing. Note that Files
        # downgrades a cross-provider transfer to a copy even when Move was chosen, so clearing the
        # iPad copy is always a separate manual step.
        INFOPLIST_KEY_LSSupportsOpeningDocumentsInPlace: YES
        # UIFileSharingEnabled has no INFOPLIST_KEY_ equivalent, so it lives in a real plist that the
        # generated keys above merge into. See Info.plist.
        INFOPLIST_FILE: Info.plist

schemes:
  MacPhotoMasterPad:
    build:
      targets:
        MacPhotoMasterPad: all
    run:
      config: Debug
      # MLX-swift legitimately calls Metal setBytes with a nil pointer for zero-element arrays, which
      # is harmless at runtime but trips Metal API Validation's hard assertion. Disable it so local
      # MLX (mlx:) inference runs under the debugger. See docs/MLX_PROVIDER.md.
      enableGPUValidationMode: disabled
    test:
      config: Debug
