.DS_Store
.build/
.swiftpm/
DerivedData/
dist/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
.vscode/
*.ipa
*.dSYM.zip
*.dSYM

# Local-only, sensitive: same convention as the phototags sibling project
Timeline*.json
*.sqlite
*.sqlite3

# Real camera files kept for hand-testing the RAW pipeline against each decoder branch.
# Hundreds of MB, and this repo is public — tests that touch them skip when they're absent.
samples/

# Python bytecode from the scripts/ helpers - generated, never source.
__pycache__/
*.pyc
