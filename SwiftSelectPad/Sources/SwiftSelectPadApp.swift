import SwiftUI

/// Entry point for the iPadOS target. See `ContentView`'s doc comment for what's actually wired up
/// so far.
@main
struct SwiftSelectPadApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
